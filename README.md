# VK MAX BOT

Бот для VK с интеграцией GigaChat AI.

## Возможности

- 📅 Просмотр расписания
- 🤖 Чат с GigaChat AI для помощи в учебе
- ⏰ Напоминания о занятиях

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Токен бота VK
BOT_TOKEN=your_bot_token_here

# GigaChat API Credentials
# Получите Client ID и Client Secret в личном кабинете GigaChat API
# Затем объедините их в формате "Client ID:Client Secret" и закодируйте в Base64
# Пример: если Client ID = "abc123", Client Secret = "xyz789"
# То нужно закодировать в Base64 строку "abc123:xyz789"
# Можно использовать онлайн-кодировщик или команду: echo -n "abc123:xyz789" | base64
GIGACHAT_CREDENTIALS=your_base64_encoded_credentials_here
```

## Настройка GigaChat API

1. Зарегистрируйтесь на [платформе разработчиков Сбера](https://developers.sber.ru/)
2. Создайте проект GigaChat API в личном кабинете
3. Получите `Client ID` и `Client Secret`
4. Объедините их в строку формата `Client ID:Client Secret`
5. Закодируйте эту строку в Base64 (можно использовать онлайн-кодировщик или команду в терминале)
6. Добавьте полученное значение в `.env` файл как `GIGACHAT_CREDENTIALS`

### Пример получения Base64 credentials:

**В Linux/Mac:**
```bash
echo -n "your_client_id:your_client_secret" | base64
```

**В Windows PowerShell:**
```powershell
[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("your_client_id:your_client_secret"))
```

**Онлайн:** Используйте любой Base64 кодировщик, например [base64encode.org](https://www.base64encode.org/)

## Запуск

### Режим разработки:
```bash
npm run dev
```

### Сборка:
```bash
npm run build
```

### Запуск в продакшене:
```bash
npm run prod
```

## Использование

1. Отправьте боту команду `/start`
2. Нажмите кнопку "🤖 GigaChat" для начала чата с нейросетью
3. Задавайте вопросы - GigaChat поможет с учебой!

## Документация GigaChat API

- [Официальная документация](https://developers.sber.ru/docs/ru/gigachat/api/reference/rest/post-token)
- [Быстрый старт](https://developers.sber.ru/docs/ru/gigachat/api/quick-start)

